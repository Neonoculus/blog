<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.blog.mapper.UserMapper">

    <!-- 插入用户 -->
    <insert id="insert" parameterType="org.example.blog.model.User">
        INSERT INTO user (id,username, password, email, sex, phone, card, avatar)
        VALUES (#{id},#{username}, #{password}, #{email}, #{sex}, #{phone}, #{card}, #{avatar})
    </insert>

    <!-- 用户登录：根据用户名和密码查询用户 -->
    <select id="login" parameterType="org.example.blog.model.User" resultType="org.example.blog.model.User">
        SELECT id, username, password, email, sex, phone, card, avatar
        FROM user
        WHERE username = #{username} AND password = #{password}
    </select>
</mapper>